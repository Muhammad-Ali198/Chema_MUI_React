export const restPoint = {
    backgroundColor: "transparent",
    boxShadow: "none",
    padding: "1rem",
    transition: "all 0.8s ease-in-out",
  };

  export const scrollActive = {
    backgroundColor: "white",
    color: "black",
    transition: "all 0.8s ease-in-out",
  };